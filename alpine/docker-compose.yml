version: '3'
services:

  #Nginx Service
  webserver:
    build: ./nginx-php-drupal
    container_name: anpd
    restart: unless-stopped
    ports:
      - "8080:8080"
      - "443:443"
    networks:
      - app-network

  #MySQL Service
  db:
    build: ./mysql
    container_name: mysql
    restart: unless-stopped
    ports:
      - "3306:3306"
    networks:
      - app-network

#Docker Networks
networks:
  app-network:
    driver: bridge